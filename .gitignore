# Ignore all env files
*.env
backend/.env
backend/environment/.env
frontend/.env
frontend/config/.env
node_modules/

# Do not push config (env/secret references)
backend/config.js

# Uploaded shift photos (stored on server; folder kept via .gitkeep)
backend/shift-photos/*
!backend/shift-photos/.gitkeep
